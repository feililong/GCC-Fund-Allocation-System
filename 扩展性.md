添加新的验证器
实现 IVerifier 接口：
javascript
class CustomVerifier {
  async verify(milestone) {
    // 自定义验证逻辑
    return {
      confidence: 85,
      sources: ['custom'],
      metadata: {}
    };
  }
}


添加新的通知渠道
实现 INotifier 接口：
javascript
class SlackNotifier {
  async send(message) {
    // Slack 发送逻辑
  }
}



支持新链
添加 ChainAdapter：
javascript
const chainAdapters = {
  ethereum: new EthereumAdapter(),
  polygon: new PolygonAdapter(),
  arbitrum: new ArbitrumAdapter(),
  // 添加新链
  optimism: new OptimismAdapter()
};
